def decelerate(from_x, to_x):
    """x-axis only whilst I figure it out"""
    return ((to_x - from_x)//5) + 4 #magic numbers make it equate to 48 - the tile width

def accelerate(from_x, to_x):
    """x-axis only"""
    pass 

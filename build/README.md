Files in here should be ran from the makefile as they are relative from the
**root** of the project. Running them from the `build` won't work.

splinter comes from (https://github.com/duolingo/splinter)
